enum ReportType {
  daily,
  weekly,
  monthly,
  custom
}

enum ScanStatus {
  pending,
  inProgress,
  completed,
  failed
}

enum RiskLevel {
  low,
  medium,
  high,
  critical
}

enum AppButtonType {
  primary,
  secondary,
  outlined,
  error
}